#!groovy

pipeline {
    agent any
    tools {
        jdk 'openjdk-11'
    }
    stages{
        stage('Git Checkout'){
            steps{
                script{
                    env.GIT_COMMIT = checkout scm
                    sh "echo ${env.GIT_COMMIT}"
                }
            }
        }
        stage('Gradlew Build'){
            steps{
                script{
                    sh """
                    chmod +x ./gradlew
                    ./gradlew build
                    """
                }
            }
        }
        stage('Test'){
            steps{
                script{
                    sh "./gradlew test"
                    sh "./gradlew check"
                    sh "echo 'generating coverage report'"
                    sh "./gradlew codeCoverageReport"
                    sh "echo 'send it to codecov'"
                    sh "curl -s https://codecov.io/bash | bash"
                }
            }
        }
        stage('Push image to Harbor'){
            steps{
                withRegistry(HARBOR_REPOSITORY, HARBOR_CREDENTIAL){
                    script{
                        sh """
                        ./gradlew jibDockerBuild
                        """
                    }
                    Image('api:latest').push("${HARBOR_REPOSITORY}/api:${env.GIT_COMMIT}")
                    Image('admin:latest').push("${HARBOR_REPOSITORY}/admin:${env.GIT_COMMIT}")
                }
            }
        }
    }
}
